openapi: 3.0.0
info:
  title: PSI Powierzenia - preferencje
  description: Detailed description can be found in the repository.
  version: 0.1.0
servers:
  - url: https://localhost/
    description: Main (production) server
  - url: http://localhost:3000
    description: Local server for development
  - url: http://localhost:6006
    description: Local server for Storybook
paths:
  /preferences/{lecturerId}:
    get:
      summary: List preferences by lecturerId.
      description: Optional extended description in CommonMark or HTML.
      operationId: listPreference
      parameters:
        - in: path
          name: lecturerId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferenceList'
  /preferences:
    post:
      summary: Find preferences.
      description: Optional extended description in CommonMark or HTML.
      operationId: findpreferences
      responses:
        '200':
          description: Template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferenceList'
components:
  schemas:
    PreferenceList:
      type: array
      items:
        $ref: '#/components/schemas/Preference'
    Preference:
      type: object
      properties:
        lecturerId:
          $ref: '#/components/schemas/LecturerId'
        courseId:
          $ref: '#/components/schemas/CourseId'
    LecturerId:
      type: string
    CourseId:
      type: string
